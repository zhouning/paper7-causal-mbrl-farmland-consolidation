# Figure Status - Paper 7 CEUS Package

No standalone figure files were found in `D:\test\paper7`.

The manuscript now defines `Fig.~\ref{fig:pipeline}` as an embedded text-box pipeline figure, so the LaTeX reference is resolved. Before final CEUS submission, replacing that placeholder with a standalone publication-quality figure is recommended:

- `figure_1_pipeline.png` or `.pdf`: trajectory collection, learned transition model, causal reward calibration, policy training in the learned environment, and real-environment evaluation.

Recommended additional figures:

- Main result comparison across greedy, model-free, model-based, and calibrated model-based methods.
- Reward-scaling grid search showing the causally derived alpha near the optimum.
- GeoFM ablation showing transition-model loss versus downstream policy quality.

Keep figure files anonymized and remove author-identifying metadata before upload.
